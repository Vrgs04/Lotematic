import {figureById} from '../catalog/figures'; import type {FigureLayout} from '../types';
export function FigureCard({id,layout='horizontal',marked=false,compact=false}:{id:number;layout?:FigureLayout;marked?:boolean;compact?:boolean}){const f=figureById(id);return <div className={`figure-card ${layout} ${marked?'marked':''} ${compact?'compact':''}`} aria-label={`${f.name}, doble`}><div className="figure-copy"><span>{f.number}</span><b>{f.name.replace(/^(El|La|Las) /,'')}</b></div><div className="figure-copy"><span>{f.number}</span><b>{f.name.replace(/^(El|La|Las) /,'')}</b></div>{marked&&<i className="bean"/>}</div>}
