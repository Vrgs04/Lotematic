import{useCallback,useRef,useState}from'react';
export function useAudioMeter(){const[level,setLevel]=useState(0);const[active,setActive]=useState(false);const stream=useRef<MediaStream|undefined>(undefined);const frame=useRef(0);const start=useCallback(async()=>{const s=await navigator.mediaDevices.getUserMedia({audio:true});stream.current=s;const ctx=new AudioContext();const analyser=ctx.createAnalyser();ctx.createMediaStreamSource(s).connect(analyser);const data=new Uint8Array(analyser.frequencyBinCount);setActive(true);const tick=()=>{analyser.getByteTimeDomainData(data);let sum=0;for(const x of data)sum+=(x-128)**2;setLevel(Math.min(100,Math.sqrt(sum/data.length)*4));frame.current=requestAnimationFrame(tick)};tick()},[]);const stop=useCallback(()=>{cancelAnimationFrame(frame.current);stream.current?.getTracks().forEach(t=>t.stop());setActive(false);setLevel(0)},[]);return{level,active,start,stop}}
